@model IEnumerable<Moviesite.Entities.Genre>

<div class="container">
    <h1>Genre</h1>

    <div class="row mb-4">
        <div class="col-12 col-md-12">
            <a class="btn-success btn float-right" asp-controller="Genre" asp-action="Create">
                <i class="fas fa-plus-circle"></i>
                &nbsp; &nbsp;
                Create
            </a>
        </div>
    </div>


    <table class="table table-bordered">

        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var genre in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => genre.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => genre.Name)
                    </td>
                    <td>
                        <a class="btn-secondary btn btn-block" asp-controller="Genre" asp-action="Edit" asp-route-id="@genre.Id">
                            <i class="fas fa-edit"></i>
                            &nbsp; &nbsp;
                            Edit
                        </a>
                        <a class="btn-info btn btn-block" asp-controller="Genre" asp-action="Details" asp-route-id="@genre.Id">
                            <i class="fas fa-info"></i>
                            &nbsp; &nbsp;
                            Details
                        </a>
                        <a class="btn-danger btn btn-block" asp-controller="Genre" asp-action="Delete" asp-route-id="@genre.Id">
                            <i class="fas fa-trash"></i>
                            &nbsp; &nbsp;
                            Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>