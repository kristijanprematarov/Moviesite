@model IEnumerable<Moviesite.Entities.Director>

<div class="container">
    <h1>Directors</h1>

    <div class="row mb-4">
        <div class="col-12 col-md-12">
            <a class="btn-success btn float-right" asp-controller="Director" asp-action="Create">
                <i class="fas fa-plus-circle"></i>
                &nbsp; &nbsp;
                Create
            </a>
        </div>
    </div>


    <table class="table table-bordered">

        <thead class="thead-dark">
            <tr>
                <th>Name</th>
                <th>Country</th>
                <th>DateBirth</th>
                <th>Short Description</th>
                <th>Gender</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var director in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => director.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => director.Country)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => director.DateBirth)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => director.ShortDescription)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => director.Gender)
                    </td>
                    <td>
                        <a class="btn-secondary btn btn-block" asp-controller="Director" asp-action="Edit" asp-route-id="@director.Id">
                            <i class="fas fa-edit"></i>
                            &nbsp; &nbsp;
                            Edit
                        </a>
                        <a class="btn-info btn btn-block" asp-controller="Director" asp-action="Details" asp-route-id="@director.Id">
                            <i class="fas fa-info"></i>
                            &nbsp; &nbsp;
                            Details
                        </a>
                        <a class="btn-danger btn btn-block" asp-controller="Director" asp-action="Delete" asp-route-id="@director.Id">
                            <i class="fas fa-trash"></i>
                            &nbsp; &nbsp;
                            Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>

</div>